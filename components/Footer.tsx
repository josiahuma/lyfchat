export default function Footer() {
  return (
    <footer className="border-t border-gray-200 mt-12 py-6 text-center text-gray-500 text-sm">
      © {new Date().getFullYear()} Lyfchat — Connect through shared passions, not appearances.
    </footer>
  );
}
